<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BaseGoodsCategoryMapper">
    
    <resultMap type="BaseGoodsCategory" id="BaseGoodsCategoryResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="parentId"    column="parent_id"    />
        <result property="name"    column="name"    />
        <result property="status"    column="status"    />
        <result property="updateTime"    column="update_time"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectBaseGoodsCategoryVo">
        select id, user_id, parent_id, name, status, update_time, create_time from base_goods_category
    </sql>

    <select id="selectBaseGoodsCategoryList" parameterType="BaseGoodsCategory" resultMap="BaseGoodsCategoryResult">
        <include refid="selectBaseGoodsCategoryVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="parentId != null "> and parent_id = #{parentId}</if>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="status != null "> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectBaseGoodsCategoryById" parameterType="Long" resultMap="BaseGoodsCategoryResult">
        <include refid="selectBaseGoodsCategoryVo"/>
        where id = #{id}
    </select>

    <insert id="insertBaseGoodsCategory" parameterType="BaseGoodsCategory" useGeneratedKeys="true" keyProperty="id">
        insert into base_goods_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="name != null">name,</if>
            <if test="status != null">status,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="name != null">#{name},</if>
            <if test="status != null">#{status},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateBaseGoodsCategory" parameterType="BaseGoodsCategory">
        update base_goods_category
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteBaseGoodsCategoryById" parameterType="Long">
        delete from base_goods_category where id = #{id}
    </delete>

    <delete id="deleteBaseGoodsCategoryByIds" parameterType="String">
        delete from base_goods_category where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>